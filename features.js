// const gfName = "Mrs Random";

// export default gfName;

const randomLovePercent = () => {
  const randomPercentage = Math.random() * 100;
  return `${~~randomPercentage}%`;
};

export default randomLovePercent;
